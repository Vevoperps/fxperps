---
tags: [frontend, seo, stable]
updated: 2026-05-21
---

# SEO & Metadata

## Site config

`src/lib/site.ts` (`siteConfig`) is the **single source of truth** for SEO —
name, description, origin URL, OG image, Twitter handle, theme colour. The
metadata generator, `robots.ts`, `sitemap.ts`, and the JSON-LD helper all read
from it. **Update the placeholder values per project.** `#todo`

`siteConfig.url` comes from `NEXT_PUBLIC_SITE_URL` (see [[environment-variables]]),
falling back to `http://localhost:3000`.

## Metadata generator

`src/utils/seo/generate-page-metadata.ts` exports two builders:

```ts
import {
  generateMetadata,
  generateViewport,
} from "@/utils/seo/generate-page-metadata";

export const metadata = generateMetadata({ title: "…", description: "…" });
export const viewport = generateViewport();
```

- `generateMetadata(props?)` → a Next.js `Metadata` object: title/description,
  OpenGraph, Twitter cards, canonical, icons, robots. `metadataBase` is **always
  set** from `siteConfig.url`, so relative URLs (OG image, canonical) resolve to
  absolute — required by social scrapers.
- `generateViewport()` → the `Viewport` export. `themeColor` lives here, **not**
  on the metadata object (Next deprecated it there).

Accepted `generateMetadata` options: `title`, `description`, `url`,
`twitterHandle`, `author`, `siteName`. (`keywords` and the non-standard `other`
tags were dropped — no SEO value.)

> [!important] `generateMetadata` names **no** images and **no** icons
> They come from the file conventions below. Anything named here wins over a
> generated file, which is exactly how the starter's placeholder assets kept
> being served after the brand existed. The `ogImage` option was removed rather
> than left as a trap.

## robots.txt & sitemap.xml

Generated by the Next file conventions:
- `src/app/robots.ts` → `/robots.txt` — allows all crawlers, points at the sitemap.
- `src/app/sitemap.ts` → `/sitemap.xml` — lists public routes (home only so far;
  add an entry per route as the site grows).

## Structured data (JSON-LD)

`src/utils/seo/structured-data.ts` → `getSiteStructuredData()` builds an
`Organization` + `WebSite` JSON-LD graph. The root layout renders it once in a
`<script type="application/ld+json">`.

## Bot detection

`src/utils/is-bot.ts` exports `isBot()` — a **server-only** async helper that
reads the `user-agent` header and returns `true` for Lighthouse, Googlebot,
PageSpeed, HeadlessChrome, GTmetrix, Pingdom, Bingbot, Yandexbot.

> [!caution]
> Calling `isBot()` (or any `headers()` reader) in a route **opts it out of
> static prerendering** — a TTFB cost for every visitor. Serving crawler-specific
> behaviour also borders on cloaking. Prefer the [[animation-system|reduced-motion]]
> path (`<ReducedMotion>`): animations only touch opacity/transform, so content
> is always in the DOM and crawlers see it regardless. Use `isBot()` sparingly.

## Icons & share card — generated, not shipped

Three Next file conventions draw the brand mark at build time with `next/og`.
Next injects the `<link>` / `<meta>` tags itself, so none of them is named in
`generateMetadata`:

| File | Route | Size |
|------|-------|------|
| `src/app/icon.tsx` | `/icon` | 64 × 64 |
| `src/app/apple-icon.tsx` | `/apple-icon` | 180 × 180 |
| `src/app/opengraph-image.tsx` | `/opengraph-image` | 1200 × 630 (also the Twitter card) |

Three things to know before editing them:

- **Colours come from `brandMark` in `lib/site.ts`, not the design tokens.**
  `next/og` rasterises in Node with no stylesheet, so CSS custom properties
  cannot reach it. `brandMark` duplicates `--raw-color-brand-600` and
  `--raw-color-white` deliberately — keep them in step.
- **Fonts are read off disk** with `fs.readFile`, not `next/font`, for the same
  reason. Both faces live in `src/app/fonts/`.
- **The share card uses `brandMark.tagline`, not `siteConfig.description`.** The
  description is written for a search result and runs long enough to overflow the
  card at display size — it did, and the text was clipped.

The starter's placeholder assets (`favicon.ico`, the `*-icon-*.png` set,
`browserconfig.xml`, `open-graph.png`) were **deleted**: they were generic, and
`src/app/favicon.ico` in particular kept winning over the generated icon. The
`public/` root now holds only `manifest.json`, which points at `/icon` and
`/apple-icon`. Site **content** assets still live under `public/assets/<section>/`
— see [[folder-structure]].

## Related

[[routing]] · [[utils]] · [[animation-system]] · [[environment-variables]] · [[decisions-log]]
